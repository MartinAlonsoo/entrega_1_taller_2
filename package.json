{
  "name": "entrega2-multisig",
  "version": "1.0.0",
  "description": "Contrato Multisig en Solidity con interfaz React",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:sepolia": "npx hardhat run scripts/deploy.js --network sepolia",
    "deploy:marketplace:sepolia": "npx hardhat run scripts/deployMarketplace.js --network sepolia",
    "node": "npx hardhat node"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "2.0.2",
    "chai": "^4.3.4",
    "ethers": "5.4.7",
    "hardhat": "2.6.8"
  },
  "dependencies": {
    "dotenv": "^10.0.0"
  }
}
